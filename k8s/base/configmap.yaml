apiVersion: v1
kind: ConfigMap
metadata:
  name: central-server-config
  namespace: neopro
  labels:
    app: central-server
data:
  NODE_ENV: "production"
  PORT: "3001"
  LOG_LEVEL: "info"
  # These values are overridden per environment
  ALLOWED_ORIGINS: "https://dashboard.neopro.fr"
