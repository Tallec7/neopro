apiVersion: v1
kind: Secret
metadata:
  name: central-server-secrets
  namespace: neopro
  labels:
    app: central-server
type: Opaque
stringData:
  # These should be managed via external secrets manager (e.g., Sealed Secrets, External Secrets)
  # Values below are placeholders
  DATABASE_URL: "postgresql://user:password@postgres:5432/neopro"
  REDIS_URL: "redis://redis:6379"
  JWT_SECRET: "change-me-in-production"
  SUPABASE_URL: "https://xxx.supabase.co"
  SUPABASE_ANON_KEY: "change-me"
  SUPABASE_SERVICE_KEY: "change-me"
